[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "boulderbuddy"
version = "0.1.0"
description = "Computer vision-based climbing movement analysis"
requires-python = ">=3.10"
